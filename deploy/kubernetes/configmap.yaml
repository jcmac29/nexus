apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-config
  namespace: nexus
data:
  APP_NAME: "Nexus"
  DEBUG: "false"
  ENVIRONMENT: "production"
  API_PREFIX: "/api/v1"
  MINIO_ENDPOINT: "minio:9000"
  MINIO_BUCKET: "nexus-media"
