{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "gridPos": {"h": 4, "w": 6, "x": 0, "y": 0},
      "id": 1,
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {"calcs": ["lastNotNull"], "fields": "", "values": false},
        "textMode": "auto"
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "expr": "sum(rate(nexus_http_requests_total[5m]))",
          "legendFormat": "Requests/sec"
        }
      ],
      "title": "Request Rate",
      "type": "stat"
    },
    {
      "gridPos": {"h": 4, "w": 6, "x": 6, "y": 0},
      "id": 2,
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": {"calcs": ["lastNotNull"], "fields": "", "values": false}
      },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(nexus_http_request_duration_seconds_bucket[5m])) by (le)) * 1000",
          "legendFormat": "P95 Latency (ms)"
        }
      ],
      "title": "P95 Latency",
      "type": "stat"
    },
    {
      "gridPos": {"h": 4, "w": 6, "x": 12, "y": 0},
      "id": 3,
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "reduceOptions": {"calcs": ["lastNotNull"], "fields": "", "values": false}
      },
      "targets": [
        {
          "expr": "sum(rate(nexus_http_requests_total{status_code=~\"5..\"}[5m])) / sum(rate(nexus_http_requests_total[5m])) * 100",
          "legendFormat": "Error Rate %"
        }
      ],
      "title": "Error Rate",
      "type": "stat"
    },
    {
      "gridPos": {"h": 4, "w": 6, "x": 18, "y": 0},
      "id": 4,
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "reduceOptions": {"calcs": ["lastNotNull"], "fields": "", "values": false}
      },
      "targets": [
        {
          "expr": "sum(nexus_agents_total{status=\"active\"})",
          "legendFormat": "Active Agents"
        }
      ],
      "title": "Active Agents",
      "type": "stat"
    },
    {
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 4},
      "id": 5,
      "options": {"legend": {"calcs": [], "displayMode": "list", "placement": "bottom"}},
      "targets": [
        {
          "expr": "sum(rate(nexus_http_requests_total[5m])) by (endpoint)",
          "legendFormat": "{{endpoint}}"
        }
      ],
      "title": "Request Rate by Endpoint",
      "type": "timeseries"
    },
    {
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 4},
      "id": 6,
      "targets": [
        {
          "expr": "histogram_quantile(0.50, sum(rate(nexus_http_request_duration_seconds_bucket[5m])) by (le, endpoint))",
          "legendFormat": "p50 {{endpoint}}"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(nexus_http_request_duration_seconds_bucket[5m])) by (le, endpoint))",
          "legendFormat": "p95 {{endpoint}}"
        }
      ],
      "title": "Latency Distribution",
      "type": "timeseries"
    },
    {
      "gridPos": {"h": 6, "w": 8, "x": 0, "y": 12},
      "id": 7,
      "targets": [
        {
          "expr": "sum(nexus_db_connections_total) by (state)",
          "legendFormat": "{{state}}"
        }
      ],
      "title": "Database Connections",
      "type": "timeseries"
    },
    {
      "gridPos": {"h": 6, "w": 8, "x": 8, "y": 12},
      "id": 8,
      "targets": [
        {
          "expr": "sum(rate(nexus_cache_hits_total[5m]))",
          "legendFormat": "Hits"
        },
        {
          "expr": "sum(rate(nexus_cache_misses_total[5m]))",
          "legendFormat": "Misses"
        }
      ],
      "title": "Cache Hit/Miss Rate",
      "type": "timeseries"
    },
    {
      "gridPos": {"h": 6, "w": 8, "x": 16, "y": 12},
      "id": 9,
      "targets": [
        {
          "expr": "sum(nexus_jobs_in_progress) by (queue)",
          "legendFormat": "{{queue}}"
        }
      ],
      "title": "Jobs In Progress",
      "type": "timeseries"
    },
    {
      "gridPos": {"h": 6, "w": 12, "x": 0, "y": 18},
      "id": 10,
      "targets": [
        {
          "expr": "sum(rate(nexus_agent_messages_total[5m])) by (direction)",
          "legendFormat": "{{direction}}"
        }
      ],
      "title": "Agent Messages",
      "type": "timeseries"
    },
    {
      "gridPos": {"h": 6, "w": 12, "x": 12, "y": 18},
      "id": 11,
      "targets": [
        {
          "expr": "sum(nexus_devices_total{status=\"online\"}) by (device_type)",
          "legendFormat": "{{device_type}}"
        }
      ],
      "title": "Online Devices",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["nexus"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "timepicker": {},
  "timezone": "",
  "title": "Nexus Overview",
  "uid": "nexus-overview",
  "version": 1
}
